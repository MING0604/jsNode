// 1.charAt()
// 取得对应下标的字符

// 2.charCodeAt()
// 取得对应下标的字符所对应的ASCII码值

// ps：String.fromCharCode()将输入的ASCII码值转化为对应字符

// 3.toLowerCase()
// 转小写

// 4.toUpperCase()
// 转大写

// 5.str1.localeCompare(str2)
// 比较str1和str2的大小关系，str1大返回1，str2大返回-1，相同返回0

// 6.indexOf()
// 查找字符串的下标，没有返回-1

// 7.lastIndexOf()
// 同上，从后面开始

// 8.str.replace(str1,str2)
// 将str中的str1替换为str2并将新字符返回，只替换一个，全部替换需要用正则

// 9.subString(beginIndex,endIndex)
// 截取，含头不含尾，第二个参数不写就是到最后

// 10.subStr(beginIndex,length)
// 截取，第二个不写到最后

// 11.split()
// 根据指定字符串分割